backup list
backup delete
backup restore
patch_t table with file, version, creation_date
patch list
patch apply
image register --work-dir
image classify --work-dir
image stats    --work-dir
image reduce   --sork-dir
image metadata --work-dir
master dark
 - master_dark_t
 - substractong
